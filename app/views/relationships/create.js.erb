$('form.new_relationship').remove();
$('section.posts h1').after("<%= escape_javascript(render 'users/unfollow') %>");
$('li.followers a').html("Followed by <%= pluralize(@user.followers.count, 'user') %>");